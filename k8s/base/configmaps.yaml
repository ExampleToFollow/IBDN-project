apiVersion: v1
kind: ConfigMap
metadata:
  name: ibdn-config
  namespace: ibdn
data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
  REQUEST_TOPIC: "flight-delay-ml-request"
  RESPONSE_TOPIC: "flight-delay-ml-response"
  MINIO_ENDPOINT: "http://minio:9000"
  TRAINING_DATA_PATH: "s3a://raw/simple_flight_delay_features.jsonl.bz2"
  MODEL_OUTPUT_BASE_PATH: "s3a://warehouse/models"
  MODEL_BASE_PATH: "s3a://warehouse/models"